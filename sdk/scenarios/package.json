{
  "name": "mosaic-scenarios",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scenarios written in prose, and the tests generated from them",
  "scripts": {
    "test": "node --experimental-transform-types --test \"*/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mosaic-ts": "file:../ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
