node_modules/

# mosaic CLI build output
sdk/cli/dist/
sdk/cli/build/
*.tsr

# ...except the archives the static viewer serves, which have to be committed for github
# pages to have anything to show
!docs/viewer/examples/**/*.tsr
